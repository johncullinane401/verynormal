raw `LOSTWOODS7_TRAP_MapScripts::
	.byte 0
`

script LOSTWOODS7_TRAP_EventScript_TeleportBack {
    lockall
    warp(MAP_DEEPWOODS4, 9, 13)
    waitstate
    if (var(VAR_EPIC_FAILS == 2)) {
        msgbox("...there's probably an easier way to do this...")
    }
    waitstate
    releaseall
}